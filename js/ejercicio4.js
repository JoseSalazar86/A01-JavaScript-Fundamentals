let cadena = "123"
let numero = 15

console.log(cadena + numero)

/* Al sumar la cadena con el numero, se concatena porque 
cuando usamos el operador + con una variable string estamos
concatenando esta variable con otra. */